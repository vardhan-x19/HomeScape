<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <% if(data) { %>
  <!-- Page Content Container -->
  <div class="flex justify-center items-center min-h-screen">
    <div class="bg-white rounded-lg shadow-lg w-full max-w-4xl p-6 flex md:flex-row flex-col">
      
      <!-- Image Section -->
      <div class="md:w-2/5 w-full flex justify-center items-center mb-6 md:mb-0">
        <img 
          src="<%= data.imgUrl%>" 
          alt="House" 
          class="w-3/5 h-3/5 object-cover rounded-lg">
      </div>

      <!-- Text Section -->
      <div class="md:w-3/5 w-full flex flex-col justify-center px-6">
        <h1 class="text-3xl font-semibold text-gray-800"><%= data.houseName %></h1>
        <p class="text-xl text-gray-600 mt-2">Location:<%= data.location %></p>
        <p class="text-lg text-gray-500 mt-2">Price: $<%= data.price %>/night</p>
        <p class="text-md text-gray-600 mt-4">
          <%= data.desc %>
        </p>
        
        <!-- Confirm Booking Button -->
         <form action="/bookings/:<%= data._id %>" method="post">
          <button type="submit" class="mt-6 bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-6 rounded-lg text-lg shadow-md">
            Confirm Booking
          </button>
         </form>
        
      </div>
      
    </div>
  </div>
  <% } %>

</body>
</html>
